@model QuanLyHocSinh.Models.ViolationType

@{
    var isEdit = Model.Id > 0;
    var pageTitle = isEdit ? "Sua loai vi pham" : "Them loai vi pham moi";
}

<div class="bg-white rounded-2xl shadow-xl w-full max-w-md mx-auto overflow-hidden">
    <!-- Header -->
    <div class="bg-green-600 px-6 py-4 flex items-center justify-between">
        <h3 class="text-white font-bold text-lg">@pageTitle</h3>
        <button type="button" onclick="closeModal()"
                class="text-white/70 hover:text-white transition-colors">
            <span class="material-symbols-outlined text-2xl">close</span>
        </button>
    </div>

    <!-- Form -->
    <form id="formViolationType" class="p-6 space-y-4">
        @Html.AntiForgeryToken()
        <input type="hidden" name="Id" value="@Model.Id" />

        <!-- Ten loai vi pham -->
        <div>
            <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                Ten loai vi pham <span class="text-red-500">*</span>
            </label>
            <input type="text"
                   name="Name"
                   id="vtName"
                   value="@Model.Name"
                   placeholder="VD: Di tre"
                   class="w-full px-4 py-2.5 border border-slate-200 rounded-xl text-sm focus:ring-2 focus:ring-green-500/20 focus:border-green-400 transition-all placeholder:text-slate-300"
                   required />
        </div>

        <!-- Muc do -->
        <div>
            <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                Muc do
            </label>
            <select name="Severity"
                    class="w-full px-4 py-2.5 border border-slate-200 rounded-xl text-sm focus:ring-2 focus:ring-green-500/20 focus:border-green-400 transition-all bg-white">
                <option value="">-- Chon muc do --</option>
                @if (Model.Severity == 1)
                {
                    <option value="1" selected>1 - Nhe</option>
                }
                else
                {
                    <option value="1">1 - Nhe</option>
                }
                @if (Model.Severity == 2)
                {
                    <option value="2" selected>2 - Trung binh</option>
                }
                else
                {
                    <option value="2">2 - Trung binh</option>
                }
                @if (Model.Severity == 3)
                {
                    <option value="3" selected>3 - Nang</option>
                }
                else
                {
                    <option value="3">3 - Nang</option>
                }
            </select>
        </div>

        <!-- Diem tru -->
        <div>
            <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                Diem tru hanh kiem <span class="text-red-500">*</span>
            </label>
            <input type="number"
                   name="DeductPoints"
                   id="vtDeductPoints"
                   value="@Model.DeductPoints"
                   placeholder="VD: 1"
                   min="0"
                   max="10"
                   class="w-full px-4 py-2.5 border border-slate-200 rounded-xl text-sm focus:ring-2 focus:ring-green-500/20 focus:border-green-400 transition-all placeholder:text-slate-300"
                   required />
            <p class="text-xs text-slate-400 mt-1">So diem tru vao hanh kiem khi vi pham</p>
        </div>

        <!-- Mo ta -->
        <div>
            <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                Mo ta chi tiet
            </label>
            <textarea name="Description"
                      id="vtDescription"
                      rows="3"
                      placeholder="Mo ta chi tiet ve loai vi pham nay..."
                      class="w-full px-4 py-2.5 border border-slate-200 rounded-xl text-sm focus:ring-2 focus:ring-green-500/20 focus:border-green-400 transition-all placeholder:text-slate-300 resize-none">@Model.Description</textarea>
        </div>

        <!-- Footer -->
        <div class="flex items-center justify-end gap-3 pt-2 border-t border-slate-100 mt-4">
            <button type="button"
                    onclick="closeModal()"
                    class="px-5 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-100 rounded-xl transition-colors">
                Huy bo
            </button>
            <button type="button"
                    onclick="submitViolationTypeForm()"
                    class="px-6 py-2.5 text-sm font-semibold text-white bg-green-600 hover:bg-green-700 rounded-xl shadow-sm transition-all">
                @(isEdit ? "Luu thay doi" : "Them loai vi pham")
            </button>
        </div>
    </form>
</div>

<script>
    function closeModal() {
        $('#modalContainer').fadeOut().addClass('hidden').removeClass('flex');
    }

    function submitViolationTypeForm() {
        var name = $('#vtName').val();

        if (!name || !name.trim()) {
            Swal.fire({
                icon: 'error',
                title: 'Loi',
                text: 'Ten loai vi pham khong duoc de trong.',
                confirmButtonColor: '#006b2c'
            });
            return;
        }

        var formData = $('#formViolationType').serialize();

        $.ajax({
            url: '@Url.Action("SaveViolationType", "ViolationType")',
            type: 'POST',
            data: formData,
            success: function(result) {
                if (result.success) {
                    Swal.fire({
                        icon: 'success',
                        title: 'Thanh cong!',
                        text: result.message,
                        confirmButtonColor: '#006b2c'
                    }).then(function() {
                        closeModal();
                        location.reload();
                    });
                } else {
                    Swal.fire({
                        icon: 'error',
                        title: 'Loi',
                        text: result.message,
                        confirmButtonColor: '#006b2c'
                    });
                }
            },
            error: function() {
                Swal.fire({
                    icon: 'error',
                    title: 'Loi',
                    text: 'Khong the luu du lieu. Vui long thu lai.',
                    confirmButtonColor: '#006b2c'
                });
            }
        });
    }
</script>
