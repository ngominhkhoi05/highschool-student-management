@*
    Popup chinh sua ho so ca nhan - Partial View load qua AJAX
*@
@model highschool_student_management.ViewModels.ProfileViewModel

<div class="bg-white w-full max-w-xl rounded-2xl shadow-2xl overflow-hidden font-['Lexend']">

    <!-- Header -->
    <div class="px-6 py-4 border-b border-slate-100 flex items-center justify-between bg-green-50/50">
        <h3 class="text-lg font-bold text-slate-800 flex items-center gap-2">
            <span class="material-symbols-outlined text-green-600">edit</span>
            Chinh sua ho so
        </h3>
        <button onclick="$('#modalContainer').fadeOut().addClass('hidden').removeClass('flex')"
                class="text-slate-400 hover:text-slate-600 transition-colors cursor-pointer">
            <span class="material-symbols-outlined">close</span>
        </button>
    </div>

    <!-- Body -->
    <div class="p-6 max-h-[70vh] overflow-y-auto">

        <form id="formEditProfile"
              asp-action="UpdateProfile"
              method="POST"
              class="space-y-4">

            <!-- Thong tin tai khoan (chi hien thi, khong cho sua) -->
            <div class="grid grid-cols-2 gap-4 pb-4 border-b border-slate-100">
                <div>
                    <p class="text-xs text-slate-400 font-semibold uppercase tracking-wide">Ten dang nhap</p>
                    <p class="text-sm font-medium text-slate-700 mt-1">@Model.Username</p>
                </div>
                <div>
                    <p class="text-xs text-slate-400 font-semibold uppercase tracking-wide">Vai tro</p>
                    <p class="text-sm font-medium text-slate-700 mt-1">@Model.RoleDisplayName</p>
                </div>
            </div>

            <!-- Email -->
            <div>
                <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Email</label>
                <input type="email"
                       name="Email"
                       value="@Model.Email"
                       class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all"
                       placeholder="Nhap dia chi email"/>
            </div>

            <!-- Ho ten -->
            <div>
                <label class="block text-xs font-bold text-slate-500 uppercase mb-1">
                    @(Model.RelatedType?.ToLower() == "student" ? "Ho va ten" : "Ho va ten")
                </label>
                <input type="text"
                       name="FullName"
                       value="@Model.FullName"
                       class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all"
                       placeholder="Nhap ho va ten"/>
            </div>

            <!-- Gioi tinh + Ngay sinh -->
            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Gioi tinh</label>
                    <select name="Gender"
                            class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all">
                        <option value="">-- Chon gioi tinh --</option>
                        <option value="1" selected="@(Model.Gender == 1)">Nam</option>
                        <option value="2" selected="@(Model.Gender == 2)">Nu</option>
                    </select>
                </div>
                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Ngay sinh</label>
                    <input type="date"
                           name="DateOfBirth"
                           value="@(Model.DateOfBirth?.ToString("yyyy-MM-dd"))"
                           class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all"/>
                </div>
            </div>

            <!-- So dien thoai -->
            <div>
                <label class="block text-xs font-bold text-slate-500 uppercase mb-1">So dien thoai</label>
                <input type="text"
                       name="Phone"
                       value="@Model.Phone"
                       class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all"
                       placeholder="Nhap so dien thoai"/>
            </div>

            <!-- Dia chi -->
            <div>
                <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Dia chi</label>
                <textarea name="Address"
                         rows="2"
                         class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all resize-none"
                         placeholder="Nhap dia chi">@Model.Address</textarea>
            </div>

            <!-- Truong rieng cua Giao vien -->
            @if (Model.RelatedType?.ToLower() == "teacher")
            {
                <div>
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-1">Chuyen mon</label>
                    <input type="text"
                           name="Specialization"
                           value="@Model.Specialization"
                           class="w-full px-4 py-2.5 rounded-lg border border-slate-200 focus:ring-2 focus:ring-green-600/20 focus:border-green-600 outline-none transition-all"
                           placeholder="VD: Toan hoc, Ngu Van..."/>
                </div>
            }

        </form>

    </div>

    <!-- Footer -->
    <div class="px-6 py-4 border-t border-slate-100 flex justify-end gap-3 bg-slate-50">
        <button type="button"
                onclick="$('#modalContainer').fadeOut().addClass('hidden').removeClass('flex')"
                class="px-5 py-2 text-slate-600 font-semibold hover:bg-slate-100 rounded-lg transition-colors">
            Huy bo
        </button>
        <button type="submit"
                form="formEditProfile"
                class="px-6 py-2 bg-green-600 text-white font-semibold rounded-lg shadow-sm hover:bg-green-700 transition-colors flex items-center gap-2">
            <span class="material-symbols-outlined text-lg">save</span>
            Luu thay doi
        </button>
    </div>

</div>
